name: ETL Pipeline

on:
  push:
    branches:
      - main

jobs:
  test-and-run:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run unit tests
      run: pytest tests
      env:
        PYTHONPATH: ${{ github.workspace }}  # Add this to include the etl module

    - name: Run ETL
      run: python run_etl.py
      env:
        PYTHONPATH: ${{ github.workspace }}  # Ensure this is set for the script as well
